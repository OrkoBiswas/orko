# SEO

- Unique route metadata and canonical-ready paths
- Person and ProfessionalService JSON-LD on public pages
- Descriptive project slugs, internal links, and project-specific metadata
- Owner-managed canonical URL, language, keywords, indexing, theme color, logo, favicon, social image, Google/Bing verification, and optional GTM container
- Server-rendered WebSite, Person/ProfessionalService, and service ItemList structured data for search, answer engines, and generative discovery
- Dynamic published-project sitemap, crawler rules, web manifest, and a factual `/llms.txt` discovery summary
- Verified public profile and marketplace links feed both visible hire-me calls to action and `sameAs` structured data
- Sitemap and robots rules
- Accessible heading hierarchy and meaningful project summaries
- Social card matched to the finished visual identity

No unverified client names, testimonials, or outcome metrics are used. Production origin is derived from the incoming host when required by metadata.
